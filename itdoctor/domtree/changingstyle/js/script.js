'use strict';

let element = document.getElementById('element');
element.style.backgroundColor = '#f00'; // Установка стилей элемента
console.log(element);

let elements = document.querySelectorAll('.workarea ul > li:first-child');
console.log(elements);